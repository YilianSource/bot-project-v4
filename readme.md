# bot-v4

## Setup

1. Clone the project.
2. Run `yarn` to install missing packages.
3. Install MySQL and setup a user, and a new database.
4. Rename `config.example.json` to `config.json` and fill in the secrets.

## Running

Run `yarn build && yarn start`.

For development purposes (hot reloading) use `yarn dev`.
